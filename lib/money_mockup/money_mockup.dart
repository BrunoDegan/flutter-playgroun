import 'package:flutter/material.dart';

class MoneyMockup extends StatelessWidget {
  const MoneyMockup({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
